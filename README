audio_transcription используется только для голосовых(на вход файл, на выходе тект)
first_upload - создание файла
get_file получить содержимое файла(Прии открытии проекта)
process_file для нейронки

1. Скачиваешь репозиторий.
2. pip install -r requirements.txt
3. uvicorn Fast:app --reload --host 0.0.0.0 --port 8080    
4. На локалхосте будет доступен по порту 8080